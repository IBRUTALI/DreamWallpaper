<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Splash application theme. -->
    <style name="Theme.DreamWallpaper.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white_lite</item>
        <item name="colorPrimaryVariant">@color/blackLite_200</item>
        <item name="colorOnPrimary">@color/white_lite</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange_lite</item>
        <item name="colorSecondaryVariant">@color/orange</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Action bar. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <!-- Status bar. -->
        <item name="android:statusBarColor">@color/white_lite</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.DreamWallpaper.ActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white_lite</item>
        <item name="colorPrimaryVariant">@color/blackLite_200</item>
        <item name="colorOnPrimary">@color/white_lite</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange_lite</item>
        <item name="colorSecondaryVariant">@color/orange</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white_lite</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Alert dialog. -->
        <item name="alertDialogTheme">@style/AlertDialogCustom</item>
    </style>

</resources>